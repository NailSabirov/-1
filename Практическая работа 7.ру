import numpy as np

A = np.array([[1, 2, 3], [1, 2, 1], [3, 2, 0]])
B = np.array([[4, 1, 2], [0, 4, 3], [1, 1, 1]])
p = np.array([0.1, 1.7, -1.5])
q = np.array([-1.6, 0.8, 1.1])

s = np.dot(np.dot(A, np.dot(B, q)) + p, p + q)

print(s)
